
Variables in iris are defined with the following syntax:

```
x: i32 = 15;
```

it's mandatory for a variable declaration to contain a starting value.

Constants are declared by adding the `const` qualifier to the variable type.

```
PI: const f32 = 6.1415;
```
