include "../std/linkedlist.iris"

main: () -> i32 {
    ll: ^LinkedListNode<u8> = nullptr;
    ll_append(&ll, 69);
    ll_append(&ll, 420);
    ll_append(&ll, 1337);
    ll_append(&ll, 69420);

    ll_print(ll);

    ll_delete(&ll);
  
    ret 0;
}
