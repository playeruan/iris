@import std

foo: (x: i32) -> (i32) -> f32 {
  cl: (a: i32) [x] -> f32 {
    ret 2x * a;
  }
  ret cl;
}

main: () -> void {
  x: const i32 = 2;
  cl: (i32) -> f32 = foo(x);

  std.println(cl(3));

  ret 0;
}
