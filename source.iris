struct Vec2 {
  x: i32;
  y: i32;
}

struct Vec3 {
  x: i32;
  y: i32;
  z: i32;
}

extern printf: (f: string, i: i32)

main: () -> i32 {
  x: i32 = 10;
  while x > 0 {
    printf("%d\n", x);
    --x;
  }
  ret 0;
}
