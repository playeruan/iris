include "std/array.iris"

main: () -> i32 {
  xs: Array<u8> = arr_create<u8>(3, [2, 4, 1]);
  arr_insert(xs, 1, 9);
  i: i32 = 0;
  while i < arr_size(xs) {
    printf("%d\n", arr_at(xs, i));
    ++i;
  }
  ret 0;
}
